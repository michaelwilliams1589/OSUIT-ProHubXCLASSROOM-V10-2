# Pro Hub X Classroom 10.2 Professional

Version 10.2 makes Delete Person more reliable. The deletion process removes the selected user's profile, role, calendar, notebooks, dashboard data, and blocks the UID without scanning the global `courseMembers` node. Existing course membership records may remain until a separate course cleanup is performed.
